/*global ToDo, $ */
var toDo = Todo || {};

Todo.taskList = {
  var tasks = {};
  addTask: function(title, description) {
    this.taskList.tasks.push({
      'title': title,
      'description': description,})
  });

  }
}
